import PageHeader from "@/components/dashboard/shared/PageHeader";

export default function AccountHeader() {
  return (
    <PageHeader
      title="Conta"
      subtitle="Gerencie seus dados e configurações da conta."
    />
  );
}